---
title: Zsh
---

## Tips <!-- Section -->

* `**/` is recursive glob search
* suffix aliases -s
* global aliases -g (can use as aliases for directories, ie `...` )
* for loop: `for i in {1..10}; do`
* Conditionals are like bash, but use `[[  ]]`
